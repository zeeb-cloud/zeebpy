# Zeeb ORM and API (zeeb_api ships inside the zeebpy distribution)
zeebpy[all] @ git+https://github.com/zeeb-cloud/zeebpy.git

# FastAPI
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# Development
pytest>=7.0.0
pytest-asyncio>=0.21.0
httpx>=0.24.0
